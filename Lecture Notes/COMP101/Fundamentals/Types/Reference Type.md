# C# Reference Types
The value of a variable containing a reference type is a reference to a storage location containing the data. 
To access the data, the runtime reads the memory location out of the variable and moves to that address to read the data (dereferencing).
The data referenced by a reference type is stored in the heap.
[[Types]]